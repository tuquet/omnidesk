import React from 'react';

export function Helper() {
  return (
    <div style={{ background: '#f0f0f0', padding: '10px', borderRadius: '4px' }}>
      I am a local helper imported via `#` alias!
    </div>
  );
}
